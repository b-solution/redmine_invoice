<h2><%= l(:label_new_payment) %></h2>
<div class="box tabular">
  <div id="all_attributes">
    <%= form_for @payment, url: project_payments_path(@project), method: 'post' do |f| %>
        <%= render :partial => 'form', :locals => {:f => f} %>
        <%= submit_tag l(:button_create) %>
    <% end %>
  </div>
</div>
